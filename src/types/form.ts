export type PointType = {
  title: string
  subtitle: string
  img: ImageData
}

export type FormProps = {
  title1: string
  title2: string
  subtitle: string
  points: Array<PointType>
  okText: string
  laterText: string
}
